import type { Metadata } from 'next';import { FinalCTA, PageShell, SectionTitle } from '@/components/site';
export const metadata:Metadata={title:'About',description:'Learn why Aera exists and how its product philosophy creates appointments without friction.'};
export default function About(){return <PageShell><section className="section pt-36"><SectionTitle kicker="About" title="We believe great software should feel invisible." text="Aera exists because booking an appointment should be one of the simplest interactions between a customer and a business."/><div className="container grid gap-4 md:grid-cols-3">{['Mission: remove friction from appointment booking.','Vision: make every service business feel modern and trusted.','Values: clarity, speed, craft, reliability, restraint.'].map(x=><div className="card p-8 text-2xl font-black" key={x}>{x}</div>)}</div></section><FinalCTA/></PageShell>}
